var buildTree = function (preorder, inorder) {
    function helper(rootPreIndex, inStart, inEnd) {
        
    }
    return helper(0, 0, inorder.length - 1);
};

console.log(buildTree(preorder, inorder));